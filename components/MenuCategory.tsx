import MenuItem from './MenuItem';
interface Props { id: string; emoji: string; title: string; note?: string; items: { name: string; price: string }[]; mustTry?: boolean; }
export default function MenuCategory({ id, emoji, title, note, items, mustTry }: Props) { return <section id={id} className="scroll-mt-28 border-b-2 border-red-100 px-4 py-14 even:bg-white sm:px-6"><div className="mx-auto max-w-5xl"><div className="flex flex-wrap items-center gap-3"><span className="text-4xl">{emoji}</span><h2 className="font-display text-4xl text-[#C0392B]">{title}</h2>{mustTry && <span className="rounded-full bg-[#F39C12] px-3 py-1 text-sm font-extrabold text-stone-900">Must Try! 🔥</span>}</div>{note && <p className="mt-3 text-sm italic leading-relaxed text-[#27AE60]">{note}</p>}<div className="mt-7 grid gap-3 md:grid-cols-2">{items.map(item => <MenuItem key={item.name} {...item} />)}</div></div></section>; }